# SSOMKIM STATE — 현재 위치 스냅샷

> **이 파일의 목적**: 세션이 끊기거나 다른 도구로 넘어갈 때, 이 한 장만 읽으면 즉시 따라잡는다.
> 새 세션/새 도구에 일을 시킬 땐 항상 이 문장으로 시작: **"STATE.md와 docs/ssomkim.html을 읽고 시작하라."**
> 최종 수정: 2026-06-13 (PR-1 완료·검증 반영 / 소유권 규칙 codify / proto 자산 등록 / 점프 proto + 물리 코어 모듈 추가)

## 0. 단일 진실 소스 (가장 중요)

**캐릭터의 정답은 `docs/ssomkim.html` 하나뿐이다.**
- 누구든(나, 페이블5, 코덱스, GPT) 작업 전 이 파일을 읽고, 작업 후 이 파일에 반영한다.
- "기억해서" 명세 쓰기 금지. 항상 이 파일 전문을 입력으로 첨부.
- 분기가 생기면 즉시 이 파일로 통합. 사고의 원인은 전부 "이 파일을 안 보고 옛 버전으로 작업"이다.

## 0.5 파일 소유권 — 쓰기 권한 경계 (절대 규칙)

사고가 반복되는 진짜 원인은 도구가 아니라 **누가 뭘 만지는지 경계가 없어서**다. 한 번 딱 정한다.

| 파일 | 쓰는 주체 | 저장소 반영 | 코덱스/GPT |
|---|---|---|---|
| **STATE.md** | **Claude만** | 유저가 업로드 | 읽기만 — 수정 금지 |
| SPEC / 카탈로그 / 핸드오프 (.md) | **Claude만** | 유저가 업로드 | 읽기만 |
| js/, reactions.json, css (실제 코드) | **코덱스** | 코덱스가 PR | — |
| docs/ssomkim.html (원본 레퍼런스) | **동결** (아무도 안 건드림) | — | 읽기만 |

**핵심 규칙 세 줄:**
1. 문서(.md)는 **Claude가 쓰고 유저가 올린다.** 코덱스·GPT는 읽기만.
2. 코드는 **코덱스가 쓴다.** Claude는 검수(읽기)만.
3. GPT는 **명세 보조.** 파일을 직접 수정/PR 하지 않는다.

> 두 도구가 같은 파일을 만지면 분기·충돌이 난다. 리마스터 분기, 구버전 분기, GPT의 STATE 수정 사고 전부 이 경계가 없어서 터졌다. 이 표가 그 방벽이다.

## 1. 한 줄 정체성

**쏨킴(본명 김솜, SSOMKIM)** — 깨물어주고 싶은(cute aggression) 크림색 솜뭉치.
겉은 말랑 캐주얼, 속은 얄미운 츤데레 + 블랙코미디. 괴롭히고→삐지고→화해하는 애증 루프가 코어.
제품 정의: **"게임은 핑계고, 쏨킴이 콘텐츠다."** 모든 판단 기준 = "이게 쏨킴을 더 살아있게 하나?"

**핵심 구조 — 대기실 중심 허브:**
- **대기실(홈)이 본론이고 목적지.** 미니게임은 곁다리. 다른 게임은 로비를 거쳐가지만, 쏨킴은 대기실에 있으러 온다.
- **"미니게임은 거들 뿐, 사람들은 대기실을 기억한다."** 게임은 클리어하면 잊히지만 관계는 기억에 남는다.
- **역설: 미니게임은 쉽고 대기실은 어렵다.** 미니게임은 룰이 다 해주고 복제됨. 대기실은 룰이 없어 어렵지만 그래서 복제 불가 = 우리 해자.
- **투자 90/10**: 대기실(캐릭터·자율성·기억·AI·톡꾸)에 90%, 미니게임은 10%이자 나중. 쉬운 곁다리부터 만드는 건 함정.
- **승부욕 OK, 단 조건**: 쏨킴과 대결 금지·실패 상태 금지·못하는 조작 금지(진입장벽 0 유지). 자기기록·수집(도감)·간접랭킹으로만.
- **상태 연속성 원칙**: 게이지·기분·기억은 어느 화면(대기실/미니게임)에서도 끊기면 안 된다 → 미니게임은 별도 페이지가 아니라 **한 PWA 안의 모드/모듈**. 캐릭터 인스턴스 하나를 공유. (기억이 대기실의 심장이므로 페이지 리로드로 상태 초기화 금지.)

## 2. 확정된 것 (변경 금지)

- **이름**: 쏨킴 / 김솜 / 영문 SSOMKIM. 호칭 3모드(쏨킴/김솜!!/솜이). 김씨 일가 확장 공식.
- **비주얼**: 가상 생물(동물 아님). 크림색 S자 실루엣, 처진 뱃살, 분홍 볼터치, 반쯤 감은 능청 눈, 얇은 외곽선, 명암 강화.
- **정수리 3종 시그니처**: 반창고(좌) + 혹(우) + 바보털(중앙).
- **후면 시그니처**: 발그레 엉덩이 반점(양쪽) + 미러된 반창고.
- **자율성 제1원리**: 명령을 다 듣지 않음. 고정 취향표(쓰다듬기O/배만지기X/"솜이"에 약함/칭찬엔 "당연한 거 아님?"/무시 못 참음/감자냐 물으면 발끈)에서 일관되게 거부.

## 3. 검증된 인터랙션 (docs/ssomkim.html에 구현됨)

| 제스처 | 동작 | 판정 수치 |
|---|---|---|
| 탭 | 찌르기 (연타 콤보) | 게이지 +6 |
| 길게 | **누르기**(찌그러짐, "찌그러진다고") ※"꼬집기/늘어난다" 아님 | 380ms 홀드 |
| 빠른 문지르기(몸통) | 간지럼 (참기→빵터짐) | 진입140/터짐460, 200ms마다 ×0.8 |
| 문지르기(정수리) | 쓰다듬기 | 게이지 -9 |
| 가로 휙 | 밀어 넘어뜨리기 (버둥→탭도움/방치) | >90px, 450ms, |dx|>|dy|×1.3 |
| 8연타 | 바보털 뽑힘→5초후 복귀 | 레어 |
| 게이지 100 | 삐짐(등 돌림)→문질러 화해 | 10초 자동복귀 |
| 게이지 70 | 분노=역삼각 변신 | |

추가 구현: 아이트래킹(눈동자 커서 추적, 화나면 시선 회피), 바보털 커서 반응, rig/그림자 레이어 분리, PWA.
물리 모델: Web Animations API + `squash` 키프레임(scale 1.26,.72 오버슈트→정착). 원칙 = **타격은 빠르게, 복원은 느리게.**

## 4. 지금 진행 단계

- **PR-1 (파일 분리 + 콘텐츠 증설): 완료·검증 끝.**
  - 구조 분리 완료: css/ js/ data/ index.html manifest sw AGENTS.md ✓
  - 사후 검증(verify) 결과: 구버전 표현(꼬집기·늘어난다고·350ms·0.82) 0개, 최신 수치(누르기 380ms·밀기 450ms·간지럼 0.8·쓰다듬기 -9·8연타·삐짐 10초) 전부 적용, **reactions.json 43개**(기준 30개 충족), 금지기능(AI 외부호출·코인·도감·먹이·새총) 유입 0.
  - ※ 이전 STATE의 "PR-1C 재작업 대기 / 누락 5요소(바보털·혹·아이트래킹·누르기·콤보)"는 **해결 완료된 옛 정보** — 폐기.
- **미해결 관리 항목 — verify 브랜치 처리**: `codex/verify-post-merge-implementation-and-update-state` 브랜치는 검증 *결과*는 유효하나, **STATE.md를 직접 재작성하고 카탈로그를 삭제**(소유권 규칙 §0.5 위반)했다. → **머지 금지, Close.** 검증 결과는 본 STATE(이 파일)에 Claude가 반영함.
- 저장소: github.com/perjoom-sketch/ssomkim

### 미니게임 프로토 — 쏨킴 물리 3종 (카탈로그 C축)

> 계획 확정: **굴리기 / 던지기 / 점프** — 셋 다 쏨킴 몸(중력·탄성·충돌)에서 직접 나오는 물리 게임. "쏨킴이라서만 되는 것" = 해자.
> **설계 원칙: 물리 코어 하나를 셋이 공유.** 굴리기로 코어 잡으면 던지기·점프는 파라미터 변경 수준. 단독 페이지 금지 → `js/games/` 모듈로 같은 앱·같은 쏨킴 인스턴스에 마운트(상태 연속성).
> **물리 코어 = `js/games/physics.js` (`AppPhysics`).** 굴리기 기준선으로 중력·마찰·탄성(반발)·물컹(soft-body) 추출. 게임별 PRESETS(roll/throw/jump)로 "파라미터만" 교체. SVG/DOM 미접촉(순수 수치 적분) → 렌더는 각 게임 담당. 점프 proto가 이 코어를 실제 로드해 검증함.

| 게임 | 물리 특성 | 시점 | 프로토 |
|---|---|---|---|
| 굴리기 | 머리쪽 휨 + 물컹 속도(통제 안 되는 주행), 수평 | 탑다운 확정 | ✅ `docs/proto/ssomkim_roll.html` (내용 미검토 / 코어 마이그레이션 대기) |
| 던지기 | 늘렸다 발사, 날아가는 게 쏨킴 자신(앵그리버드형 조준), 포물선 | — | ✅ `docs/proto/ssomkim_throw.html` (내용 미검토 / 코어 마이그레이션 대기) |
| 점프 | 눌렀다 튕겨 오르기(트램펄린/높이), 수직 반동 | 측면 확정 | ✅ `docs/proto/ssomkim_jump.html` (코어 사용 / 정수리 3종 시그니처·혹 포함) |

## 5. 도구 현황 (2026-06-13)

- 페이블5: 다운(쿼터). 비주얼/대량생성 담당.
- 데빈/안티그래비티: 쿼터 한도.
- 코덱스: 복귀. 큰 범위에서 뻗으므로 **작업 잘게 쪼개서** 줄 것. 구버전 분기 주의. **STATE/문서(.md)는 절대 손대지 않게 핸드오프에 명시**(§0.5).
- Claude(Opus 4.8): 전략·검수·분기통합·정밀수정 + **문서(.md) 작성 전담**. 세션이 짧으니 맥락은 이 파일에 저장하고 매번 입력으로 받는다.

**역할 분리**: 대량생성/그림→페이블5, 코드분리/PR→코덱스, 판단·검수·통합·문서작성→Claude.

## 6. 다음 할 일

1. PR-1 완료 ✓ / verify 브랜치 Close.
2. **미니게임 물리 3종 마무리**: ① throw·roll 프로토 내용 검토 ② 점프 프로토 제작 ✅ ③ 물리 코어 `js/games/physics.js` 모듈화 ✅ → **다음: roll·throw 프로토를 코어(AppPhysics)로 마이그레이션**(코덱스), 그 후 3종을 `js/games/`에서 같은 쏨킴 인스턴스에 모드로 마운트.
3. 대기실 콘텐츠를 reactions.json 카드로 추가 (카탈로그 A·B부터).
4. 미니게임 소재 = 카탈로그(docs/SSOMKIM_CONTENT_CATALOG.md) 참조.
5. 상표 출원 (빈둥이 교훈 — 이름 공개됐으니 서두를 것).

### 허브 구조 백로그
**[대기실 — 본론, 90% 투자]** 쏨킴이랑 같이 있는 상시공간. 진입장벽 0, 실패 없음.
- 만지기: 찌르기/누르기/간지럼/쓰다듬기/밀기 (구현됨)
- 채팅·음성 명령: "한 바퀴 돌아봐" "허리 숙여봐" → 기분따라 수행/거부/역제안 (자율성 제1원리)
- 톡꾸: 방치하면 쏨킴이 먼저 말 검 (무시 못 참는 성격과 연결)
- 슬랩스틱: 깨물기 모션 / 떨어진 바보털 직접 주워주기 / 혹 동적 증가 / (throw·roll proto가 여기 후보)
- AI 레이어(PR-3): T4 정색 멘트, 기억 스키마(양방향 기억이 대기실의 심장)

**[미니게임 — 곁다리, 10%, 나중. 모듈로 입장]**
- 반응 놀이: 말 시키기 / 쏨킴을 웃겨라 / 눈싸움
- 보드 놀이: 윷/주사위/사다리/알까기 (쏨킴이 상대, 턴제, 방향키·마우스 OK)
- 수집: 레어 반응 도감 (승부욕·바이럴·리텐션을 진입장벽 0 안 건드리고 잡는 핵심 축)

## 7. 운영 규율

1. **단일 진실 소스**: docs/ssomkim.html. 모든 도구 여기서 읽고 여기에 쓴다.
2. **소유권 경계(§0.5) 준수**: 문서=Claude, 코드=코덱스, 원본=동결. 두 도구가 같은 파일 만지지 않게.
3. **명세에 기준 전문 첨부**: "기억해서" 금지, "이 파일 보고".
4. **쏨킴은 반복수정형 작업**(디스원=누적생성형과 다름) → 매 작업이 현재상태 의존 → 단일소스가 생명.
5. **모든 산출물은 만들 때 [커밋 + STATE 한 줄]**. 안 그러면 다음 세션이 장님 됨(throw·roll proto가 그 사례 — 만들었으나 미등록으로 맥락 끊김).
